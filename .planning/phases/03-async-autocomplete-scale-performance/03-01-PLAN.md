---
phase: 03-async-autocomplete-scale-performance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - django_smart_filters/autocomplete.py
  - tests/test_autocomplete.py
autonomous: true
requirements: [FILT-02, PERF-01, PERF-02, PERF-04]
must_haves:
  truths:
    - "User typing a query receives matching autocomplete options from server-side search (D-01, D-04)."
    - "User can request next pages with deterministic order and bounded page size (D-07, D-08)."
    - "Autocomplete does not materialize full option sets in memory for high-cardinality fields (D-09, D-12)."
  artifacts:
    - path: "django_smart_filters/autocomplete.py"
      provides: "Validated autocomplete request parsing, paginated option search, and minimal response serialization"
      exports: ["AutocompleteRequest", "AutocompleteResultPage", "parse_autocomplete_request", "search_autocomplete_options"]
    - path: "tests/test_autocomplete.py"
      provides: "TDD coverage for validation, min-query gate, pagination, deterministic ordering, and payload shape"
  key_links:
    - from: "parse_autocomplete_request"
      to: "search_autocomplete_options"
      via: "validated request object"
      pattern: "parse.*query.*page.*limit"
    - from: "search_autocomplete_options"
      to: "queryset.values_list/slicing"
      via: "server-side filtered pagination"
      pattern: "order_by.*offset.*limit"
---

<objective>
Define and verify the backend autocomplete contract and search engine for high-cardinality filters.

Purpose: Deliver the performance-critical server-side core first so admin/UI wiring can consume a stable, validated interface.
Output: `autocomplete.py` request/response/search primitives with focused tests that lock behavior before integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-async-autocomplete-scale-performance/03-CONTEXT.md
@django_smart_filters/contracts.py
@django_smart_filters/validation.py
@django_smart_filters/state.py
@tests/test_query.py

<interfaces>
From django_smart_filters/contracts.py:
```python
@dataclass(frozen=True)
class FilterSpec:
    field_name: str
    alias: str | None
    filter_kind: str
    query_hook: QueryHook | None
    widget_hook: WidgetHook | None
    param_name: str
```

From django_smart_filters/validation.py:
```python
SUPPORTED_FILTER_KINDS = {
    "dropdown", "autocomplete", "multi_select", "date_range", "numeric_range", "boolean_toggle"
}

def validate_filter_spec(spec: FilterSpec) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: lock autocomplete request and pagination behavior in tests</name>
  <read_first>
    django_smart_filters/contracts.py
    django_smart_filters/validation.py
    tests/test_query.py
  </read_first>
  <files>tests/test_autocomplete.py</files>
  <behavior>
    - Test 1: Valid request parses `query`, `page`, and `limit` with defaults (per D-02, D-07, D-08).
    - Test 2: Too-short query returns empty option page without DB scan trigger (per D-05, D-12).
    - Test 3: Result serialization only includes `id`, `value`, and `label` keys (per D-03).
    - Test 4: Pagination is deterministic across pages when ordered by label then pk (per D-07).
    - Test 5: Invalid request params fail fast with explicit error messages (per D-13).
  </behavior>
  <action>Create `tests/test_autocomplete.py` with RED tests for request parsing, min-query threshold, conservative default page size, deterministic page slicing, and minimal payload shape. Use a lightweight `RecordingQuerySet`/fake iterable pattern consistent with existing test style so behavior is verifiable without full DB fixtures.</action>
  <acceptance_criteria>
    - `tests/test_autocomplete.py` contains tests named `test_parse_autocomplete_request_*` and `test_search_autocomplete_options_*`.
    - Tests assert default `limit` is conservative (for example 20) and enforce an upper cap (for example 50).
    - Tests assert response option dict keys equal exactly `{"id", "value", "label"}`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_autocomplete.py -x</automated>
  </verify>
  <done>Comprehensive failing tests exist for all Phase 3 backend contract behaviors before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement validated server-side autocomplete search core</name>
  <read_first>
    tests/test_autocomplete.py
    django_smart_filters/contracts.py
    django_smart_filters/state.py
  </read_first>
  <files>django_smart_filters/autocomplete.py</files>
  <action>Implement backend primitives in `autocomplete.py` for: (a) request parsing with `query`, `page`, `limit`, `field_name`, and optional active-state passthrough metadata; (b) fail-fast validation of page/limit/query values (per D-13); (c) min-query short-circuit (per D-05); (d) server-side search + deterministic ordering + offset/limit slicing (per D-04, D-07, D-12); and (e) minimal option serializer returning only `id`, `value`, `label` (per D-03). Keep defaults conservative and bounded (per D-08). Use declared `FilterSpec` context instead of hardcoded field assumptions (per D-01, D-02).</action>
  <acceptance_criteria>
    - `django_smart_filters/autocomplete.py` exports `parse_autocomplete_request` and `search_autocomplete_options`.
    - Module defines explicit constants for min query length and default/max page size.
    - Search path uses server-side filtering and paginated slicing (contains offset/limit calculation and deterministic ordering call).
    - No serializer output includes fields beyond `id`, `value`, `label`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_autocomplete.py -x</automated>
  </verify>
  <done>All autocomplete core tests pass, with validated, paginated, deterministic, memory-bounded backend behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → autocomplete parser | Untrusted query/page/limit text enters server-side filtering path |
| parser/search core → queryset | User-supplied inputs influence DB query construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `parse_autocomplete_request` | mitigate | Strictly validate numeric bounds and query length; reject invalid/negative page/limit with explicit ValueError (D-13). |
| T-03-02 | D | `search_autocomplete_options` | mitigate | Enforce min query + conservative page size cap and deterministic pagination to bound work per request (D-05, D-08, D-12). |
| T-03-03 | I | option serializer | mitigate | Return minimal option schema (`id`, `value`, `label`) only; never leak model internals or extra fields (D-03). |
</threat_model>

<verification>
Run `python -m pytest tests/test_autocomplete.py -x`.
</verification>

<success_criteria>
- Autocomplete backend primitives are test-locked and passing.
- Request parsing/validation, min-query gating, deterministic pagination, and minimal payload shape are all enforced.
- Backend contract is ready for admin endpoint + UI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-async-autocomplete-scale-performance/03-01-SUMMARY.md`
</output>
