---
phase: 01-declarative-filter-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django_smart_filters/contracts.py
  - django_smart_filters/declarations.py
  - django_smart_filters/builder.py
  - django_smart_filters/validation.py
  - django_smart_filters/params.py
  - django_smart_filters/__init__.py
  - tests/test_declarations.py
  - tests/test_validation.py
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - "Developer can declare a smart filter using list_filter-compatible class declaration."
    - "Developer can declare equivalent filter behavior with a fluent builder declaration."
    - "Invalid filter definitions fail fast with actionable startup/import-time errors."
  artifacts:
    - path: "django_smart_filters/contracts.py"
      provides: "Normalized filter contracts and extensibility hooks"
      contains: "class FilterSpec"
    - path: "django_smart_filters/declarations.py"
      provides: "Class-style declaration normalization"
      contains: "def normalize_class_declaration"
    - path: "django_smart_filters/builder.py"
      provides: "Fluent declaration API"
      contains: "class Filter"
    - path: "django_smart_filters/validation.py"
      provides: "Fail-fast validation"
      contains: "class FilterValidationError"
  key_links:
    - from: "django_smart_filters/declarations.py"
      to: "django_smart_filters/contracts.py"
      via: "normalize_* functions returning FilterSpec"
      pattern: "-> FilterSpec"
    - from: "django_smart_filters/builder.py"
      to: "django_smart_filters/declarations.py"
      via: "builder output normalized by shared path"
      pattern: "normalize_builder_declaration"
    - from: "django_smart_filters/validation.py"
      to: "django_smart_filters/contracts.py"
      via: "validate spec fields and hooks"
      pattern: "validate_filter_spec"
---

<objective>
Define the phase-1 API contract and normalization core so both class-style and fluent declarations resolve to one validated internal filter spec.

Purpose: Establish a stable, Django-native declaration foundation before changelist UX and async filtering phases consume this contract.
Output: Core API modules, fail-fast validation, and unit tests proving declaration parity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-declarative-filter-api-foundation/01-CONTEXT.md
@docs/project_description.md
</context>

<tasks>

<task type="auto">
  <name>task 1: define normalized filter contract and deterministic parameter strategy</name>
  <read_first>
    - .planning/phases/01-declarative-filter-api-foundation/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - django_smart_filters/contracts.py
    - django_smart_filters/params.py
  </read_first>
  <files>django_smart_filters/contracts.py, django_smart_filters/params.py, django_smart_filters/__init__.py</files>
  <action>Create `FilterSpec`, `QueryHook`, and `WidgetHook` contracts per D-05 and D-06, plus a deterministic parameter naming helper per D-04 that derives names from field identity (`{field}` for single value, `{field}__in` for multivalue) and supports explicit alias override (`alias` takes precedence). Export these public contracts from `django_smart_filters/__init__.py` and do not add theme/rendering concerns in this phase.</action>
  <acceptance_criteria>
    - `django_smart_filters/contracts.py` contains `class FilterSpec` with fields for `field_name`, `alias`, `filter_kind`, `query_hook`, and `widget_hook`.
    - `django_smart_filters/params.py` contains `def resolve_param_name(` and implements alias precedence before fallback derivation.
    - `django_smart_filters/__init__.py` exports `FilterSpec` and hook protocol/type names.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_declarations.py::test_param_name_deterministic_and_alias_override -q</automated>
  </verify>
  <done>Core contract files exist with explicit query/widget hook seams and deterministic parameter naming behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement class + fluent declaration normalization through one internal spec</name>
  <read_first>
    - django_smart_filters/contracts.py
    - .planning/phases/01-declarative-filter-api-foundation/01-CONTEXT.md
    - django_smart_filters/declarations.py
    - django_smart_filters/builder.py
    - tests/test_declarations.py
  </read_first>
  <files>django_smart_filters/declarations.py, django_smart_filters/builder.py, tests/test_declarations.py</files>
  <behavior>
    - Test 1: class-style declaration normalizes to `FilterSpec` with expected field_name/filter_kind/param_name.
    - Test 2: fluent declaration (`Filter.field("status").dropdown()`) normalizes to equivalent `FilterSpec`.
    - Test 3: mixed declarations remain list_filter-compatible structures and preserve order.
  </behavior>
  <action>Implement `normalize_class_declaration` and `normalize_builder_declaration` so D-01 and D-02 are fully satisfied through a shared normalization path producing identical `FilterSpec` values for equivalent declarations. Provide fluent `Filter.field(...).{dropdown|autocomplete|multi_select|date_range|numeric_range|boolean_toggle}()` builders as declarative constructors only (no UI/rendering logic).</action>
  <acceptance_criteria>
    - `tests/test_declarations.py` contains parity tests asserting class-style and fluent-style equivalence.
    - `django_smart_filters/declarations.py` and `django_smart_filters/builder.py` both return `FilterSpec` objects.
    - No task code introduces non-Django `list_filter` replacement abstractions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_declarations.py -q</automated>
  </verify>
  <done>Both declaration styles compile into one normalized internal representation without API drift.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: add fail-fast validation with actionable error messages</name>
  <read_first>
    - django_smart_filters/contracts.py
    - django_smart_filters/declarations.py
    - django_smart_filters/validation.py
    - tests/test_validation.py
    - .planning/phases/01-declarative-filter-api-foundation/01-CONTEXT.md
  </read_first>
  <files>django_smart_filters/validation.py, django_smart_filters/declarations.py, tests/test_validation.py</files>
  <behavior>
    - Test 1: invalid field_name (empty/None) raises `FilterValidationError` at normalization time.
    - Test 2: duplicate resolved query parameter names in one declaration set raise a clear collision error.
    - Test 3: unsupported filter_kind raises a message that includes the invalid value and supported options.
  </behavior>
  <action>Implement startup/import-time validation per D-03 by calling `validate_filter_spec` during normalization, raising `FilterValidationError` with concrete fix guidance (field name, alias conflict, supported kinds). Include D-04 collision checks after alias + deterministic derivation resolution so multi-filter declarations fail fast before runtime queryset evaluation.</action>
  <acceptance_criteria>
    - `django_smart_filters/validation.py` defines `FilterValidationError` and `validate_filter_spec`.
    - Validation is invoked from normalization flow, not deferred to filter execution.
    - Error messages include invalid value and remediation hint text.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_validation.py -q</automated>
  </verify>
  <done>Invalid declarations are blocked early with actionable errors and deterministic naming collision protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer declarations → normalization engine | Untrusted/incorrect config input crosses into contract normalization and validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | declaration normalization | mitigate | Validate filter_kind against allowlist and reject unknown values via `FilterValidationError` |
| T-01-02 | D | parameter naming resolver | mitigate | Detect alias/derived-name collisions pre-runtime and fail fast to avoid ambiguous queryset filtering |
| T-01-03 | I | validation errors | mitigate | Error messages expose only declaration metadata (no data values), preserving low information disclosure risk |
| T-01-04 | R | startup validation path | accept | Phase-1 scope is local library configuration with no remote identity context; formal audit trail deferred to later phases |
</threat_model>

<verification>
Run:
- `python -m pytest tests/test_declarations.py -q`
- `python -m pytest tests/test_validation.py -q`

Both must pass with no skipped parity or validation tests.
</verification>

<success_criteria>
- Class-style and fluent declarations normalize into equivalent `FilterSpec` outputs.
- Fail-fast validation catches invalid and colliding filter declarations before runtime filtering.
- Deterministic parameter naming with alias override is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-declarative-filter-api-foundation/01-declarative-filter-api-foundation-01-SUMMARY.md`
</output>
