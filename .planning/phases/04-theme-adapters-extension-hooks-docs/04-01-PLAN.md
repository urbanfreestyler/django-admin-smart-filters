---
phase: 04-theme-adapters-extension-hooks-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - django_smart_filters/contracts.py
  - django_smart_filters/builder.py
  - django_smart_filters/declarations.py
  - django_smart_filters/registry.py
  - django_smart_filters/__init__.py
  - tests/test_extension_registry.py
  - tests/test_declarations.py
autonomous: true
requirements: [EXT-01, EXT-02]
must_haves:
  truths:
    - "Developer can define a custom filter component contract and register it without patching core query logic."
    - "Developer can attach custom query and widget hooks through a supported declaration/builder API."
    - "Unknown component keys fail fast with clear validation errors."
  artifacts:
    - path: "django_smart_filters/registry.py"
      provides: "Component registration/resolution API for custom filter components"
      exports: ["register_filter_component", "resolve_filter_component", "clear_filter_component_registry"]
    - path: "django_smart_filters/contracts.py"
      provides: "Base extension contracts for FilterComponent and hook-capable specs"
      contains: "class FilterComponent"
    - path: "tests/test_extension_registry.py"
      provides: "Executable coverage for register/resolve/fail-fast behavior and hook wiring"
  key_links:
    - from: "django_smart_filters/declarations.py"
      to: "django_smart_filters/registry.py"
      via: "component key resolution during normalization"
      pattern: "resolve_filter_component"
    - from: "django_smart_filters/builder.py"
      to: "django_smart_filters/contracts.py"
      via: "query_hook/widget_hook assignment"
      pattern: "query_hook|widget_hook"
---

<objective>
Create the extension foundation required for custom filter components and pluggable query/widget behavior.

Purpose: Establish stable contracts first so theme integration and docs can build on non-breaking extension primitives.
Output: Extension contracts, component registry, and declaration/builder hook wiring with tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-built-in-filters-changelist-ux/02-02-SUMMARY.md
@django_smart_filters/contracts.py
@django_smart_filters/declarations.py
@django_smart_filters/builder.py
@django_smart_filters/validation.py

<interfaces>
From django_smart_filters/contracts.py:
```python
@dataclass(frozen=True)
class FilterSpec:
    field_name: str
    alias: str | None
    filter_kind: str
    query_hook: QueryHook | None
    widget_hook: WidgetHook | None
    param_name: str
```

From django_smart_filters/declarations.py:
```python
def normalize_declarations(declarations: Iterable[object]) -> list[FilterSpec]: ...
```

From django_smart_filters/builder.py:
```python
class Filter:
    @staticmethod
    def field(field_name: str) -> _FilterFieldBuilder: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: lock extension registry and hook API behavior in tests</name>
  <files>tests/test_extension_registry.py, tests/test_declarations.py</files>
  <behavior>
    - Test 1: registering a custom component key resolves to the same component class.
    - Test 2: duplicate registration of same key raises explicit ValueError.
    - Test 3: unknown component key in declaration normalization fails fast with actionable error.
    - Test 4: fluent builder can bind query_hook and widget_hook and normalized spec keeps them.
  </behavior>
  <action>Add RED tests for component registration/resolution lifecycle and extension-hook declaration wiring. Reuse existing lightweight test style (no DB fixtures). Do not add theme behavior in this task; this task only locks extension contracts and fail-fast semantics.</action>
  <verify>
    <automated>python -m pytest tests/test_extension_registry.py tests/test_declarations.py -x</automated>
  </verify>
  <done>Failing tests precisely define custom component + hook extension behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement FilterComponent contract, registry module, and declaration/builder wiring</name>
  <files>django_smart_filters/contracts.py, django_smart_filters/registry.py, django_smart_filters/declarations.py, django_smart_filters/builder.py, django_smart_filters/__init__.py</files>
  <action>Create a `FilterComponent` base contract in `contracts.py` and a new `registry.py` module with register/resolve/clear helpers. Update normalization path to optionally accept component keys and resolve them through registry. Extend fluent builder API with explicit hook methods (for example, `with_query_hook(...)` and `with_widget_hook(...)`) that preserve existing behavior and keep backward compatibility with current declarations. Export new public extension primitives from `__init__.py`. Keep validation fail-fast and deterministic.</action>
  <verify>
    <automated>python -m pytest tests/test_extension_registry.py tests/test_declarations.py -x</automated>
  </verify>
  <done>Custom components and hook wiring are first-class, test-backed extension points for downstream plans.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_extension_registry.py tests/test_declarations.py -x`.
</verification>

<success_criteria>
- Developers can register and resolve a custom filter component key.
- Builder/declaration paths support pluggable query/widget hooks without manual core patching.
- Invalid extension keys fail fast with explicit errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-theme-adapters-extension-hooks-docs/04-01-SUMMARY.md`
</output>
