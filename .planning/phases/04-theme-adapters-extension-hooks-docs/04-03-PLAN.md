---
phase: 04-theme-adapters-extension-hooks-docs
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - docs/extension_hooks.md
  - docs/theme_adapters.md
  - docs/examples.md
  - docs/project_description.md
  - tests/test_docs_examples.py
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Developer can copy documented examples to implement core v1 flows without guessing hidden contracts."
    - "Docs include explicit custom filter component registration and hook usage examples that match implemented API."
    - "Docs include theme adapter override flow with concrete template paths and fallback behavior."
  artifacts:
    - path: "docs/extension_hooks.md"
      provides: "Custom FilterComponent + query/widget hook implementation guide"
    - path: "docs/theme_adapters.md"
      provides: "Theme adapter architecture, registration, and template override structure"
    - path: "tests/test_docs_examples.py"
      provides: "Executable checks ensuring copyable examples stay in sync"
  key_links:
    - from: "docs/examples.md"
      to: "public package exports"
      via: "import snippets"
      pattern: "from django_smart_filters import"
    - from: "docs/theme_adapters.md"
      to: "theme adapter templates"
      via: "documented path mapping"
      pattern: "templates/admin/django_smart_filters/theme"
---

<objective>
Publish official, copyable docs for extension hooks and theme adapter usage, backed by executable example checks.

Purpose: Convert new extension/theming capabilities into practical adoption guidance that developers can use immediately.
Output: Focused docs and doc-example verification tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/project_description.md
@django_smart_filters/__init__.py
@django_smart_filters/contracts.py
@django_smart_filters/registry.py
@django_smart_filters/theme.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add doc-example verification tests for extension and adapter snippets</name>
  <files>tests/test_docs_examples.py</files>
  <behavior>
    - Test 1: extension docs snippets import public contracts from package without missing symbols.
    - Test 2: custom component registration snippet executes against registry API.
    - Test 3: theme adapter snippet references valid adapter/template path contracts.
  </behavior>
  <action>Create RED tests that execute or parse representative code snippets from docs to prevent docs drift. Keep tests lightweight and deterministic (no external services, no browser automation).</action>
  <verify>
    <automated>python -m pytest tests/test_docs_examples.py -x</automated>
  </verify>
  <done>Failing tests define required correctness of documentation examples.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: write extension and theme adapter docs with copyable core flow examples</name>
  <files>docs/extension_hooks.md, docs/theme_adapters.md, docs/examples.md, docs/project_description.md</files>
  <action>Write docs covering: (a) how to implement/register a custom filter component through the new registry and extension contracts; (b) how to attach `query_hook` and `widget_hook` in class/fluent declaration flows; (c) how to configure and override theme adapter templates using documented path conventions; and (d) one end-to-end v1 example combining built-ins + custom component + adapter override. Update `project_description.md` to reference these official docs. Ensure snippets are directly copyable and match the implemented API names exactly.</action>
  <verify>
    <automated>python -m pytest tests/test_docs_examples.py -x</automated>
  </verify>
  <done>Official docs are complete, copyable, and kept in sync by executable checks.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_docs_examples.py -x`.
</verification>

<success_criteria>
- Extension and adapter docs provide end-to-end copyable guidance for core v1 flows.
- Documentation examples are test-validated against real API contracts.
- Developers can follow docs to build custom filters and themed rendering without undocumented steps.
</success_criteria>

<output>
After completion, create `.planning/phases/04-theme-adapters-extension-hooks-docs/04-03-SUMMARY.md`
</output>
