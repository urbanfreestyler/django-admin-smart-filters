---
phase: 04-theme-adapters-extension-hooks-docs
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - django_smart_filters/theme.py
  - django_smart_filters/admin.py
  - django_smart_filters/templates/admin/django_smart_filters/filter_controls.html
  - django_smart_filters/templates/admin/django_smart_filters/active_filters_bar.html
  - django_smart_filters/templates/admin/django_smart_filters/theme/default/filter_controls.html
  - django_smart_filters/templates/admin/django_smart_filters/theme/default/active_filters_bar.html
  - tests/test_theme_adapters.py
  - tests/test_admin_filters.py
autonomous: true
requirements: [THEM-02, THEM-03]
must_haves:
  truths:
    - "Developer can select a theme adapter so control and active-bar rendering works without changing filter/query internals."
    - "Developer can override adapter templates through documented template paths rather than hardcoded theme conditions."
    - "Default admin behavior remains the canonical fallback when no explicit adapter is configured."
  artifacts:
    - path: "django_smart_filters/theme.py"
      provides: "Theme adapter contract and resolver"
      exports: ["ThemeAdapter", "resolve_theme_adapter"]
    - path: "django_smart_filters/admin.py"
      provides: "Adapter-aware template/render selection in SmartFilterAdminMixin"
    - path: "tests/test_theme_adapters.py"
      provides: "Behavioral tests for adapter selection, fallback, and template overrides"
  key_links:
    - from: "django_smart_filters/admin.py"
      to: "django_smart_filters/theme.py"
      via: "adapter resolution in changelist rendering"
      pattern: "resolve_theme_adapter"
    - from: "theme adapter template map"
      to: "django template loader"
      via: "render_to_string(adapter_template)"
      pattern: "render_to_string"
---

<objective>
Implement adapter-based theme rendering so UI output can vary by theme without changing filtering semantics.

Purpose: Satisfy theme compatibility requirements while preserving existing query/state architecture and additive admin integration.
Output: Theme adapter abstraction, admin wiring, adapter templates, and tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-built-in-filters-changelist-ux/02-02-SUMMARY.md
@.planning/phases/02-core-built-in-filters-changelist-ux/02-03-SUMMARY.md
@django_smart_filters/admin.py
@django_smart_filters/templates/admin/django_smart_filters/filter_controls.html
@django_smart_filters/templates/admin/django_smart_filters/active_filters_bar.html

<interfaces>
From django_smart_filters/admin.py:
```python
class SmartFilterAdminMixin:
    smart_filter_controls_template = "admin/django_smart_filters/filter_controls.html"
    smart_filter_active_bar_template = "admin/django_smart_filters/active_filters_bar.html"
    def render_smart_filter_controls(self, controls: list[dict[str, Any]]) -> str: ...
    def render_smart_filter_active_bar(self, chips: list[dict[str, Any]], reset_all_url: str) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add adapter behavior tests for fallback and template override contracts</name>
  <files>tests/test_theme_adapters.py, tests/test_admin_filters.py</files>
  <behavior>
    - Test 1: default adapter resolves when no custom adapter is configured.
    - Test 2: custom adapter can provide alternate controls/active-bar template paths.
    - Test 3: changelist context/rendered html uses adapter-selected template paths.
    - Test 4: query/filter behavior remains unchanged while adapter changes only rendering paths.
  </behavior>
  <action>Create RED tests that lock adapter interface and fallback semantics before implementation. Keep tests additive to existing admin integration tests and verify no hardcoded branch by theme name in business logic.</action>
  <verify>
    <automated>python -m pytest tests/test_theme_adapters.py tests/test_admin_filters.py -x</automated>
  </verify>
  <done>Failing tests establish adapter selection and template override behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement theme adapter contract and admin adapter wiring</name>
  <files>django_smart_filters/theme.py, django_smart_filters/admin.py, django_smart_filters/templates/admin/django_smart_filters/theme/default/filter_controls.html, django_smart_filters/templates/admin/django_smart_filters/theme/default/active_filters_bar.html, django_smart_filters/templates/admin/django_smart_filters/filter_controls.html, django_smart_filters/templates/admin/django_smart_filters/active_filters_bar.html</files>
  <action>Add a dedicated theme adapter contract/resolver module (`theme.py`) that maps rendering contract to template paths and optional class-map metadata. Update `SmartFilterAdminMixin` to resolve adapter once per request and render controls/active bar through adapter template references while preserving existing control/chip context structures. Provide default adapter templates under `templates/admin/django_smart_filters/theme/default/` and keep legacy template paths as compatibility wrappers/includes so downstream overrides remain non-breaking.</action>
  <verify>
    <automated>python -m pytest tests/test_theme_adapters.py tests/test_admin_filters.py -x</automated>
  </verify>
  <done>Theme adapter architecture is active, default-safe, and template-override-friendly.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_theme_adapters.py tests/test_admin_filters.py -x`.
</verification>

<success_criteria>
- Theme-specific rendering is selected through adapters, not hardcoded conditional logic.
- Template override paths are stable and documented by structure.
- Existing default-admin integration remains functional.
</success_criteria>

<output>
After completion, create `.planning/phases/04-theme-adapters-extension-hooks-docs/04-02-SUMMARY.md`
</output>
