---
phase: "03-automated-quality-gates"
plan: "02"
type: "execute"
wave: 2
depends_on: ["03-01"]
files_modified: [".github/workflows/ci.yml"]
autonomous: true
requirements: ["REL-03"]
must_haves:
  truths:
    - "CI workflow triggers on push and pull request"
    - "CI tests multiple python and django versions"
    - "CI enforces quality gates before allowing merge"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow"
      contains: "pytest"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Codebase"
      via: "actions checkout"
      pattern: "actions/checkout"
---

<objective>
Configure Continuous Integration (GitHub Actions) to automatically run quality gates.

Purpose: To ensure every commit and PR is automatically verified against our standards (pytest, ruff, mypy).
Output: A complete GitHub Actions workflow configuration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-automated-quality-gates/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: create CI workflow file</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create a GitHub Actions workflow that runs on push to `main` and all pull requests. Set up a matrix for Python (3.12, 3.13, 3.14) and run the following checks: `pip install -r requirements-dev.txt`, `ruff check .`, `ruff format --check .`, `mypy .`, and `pytest`.</action>
  <verify>
    <automated>ls -la .github/workflows/ci.yml</automated>
  </verify>
  <done>Workflow file is successfully created with correct YAML structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub→Runner | PRs from untrusted forks executing code in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Elevation of Privilege | CI Workflow | mitigate | Do not use pull_request_target for executing untrusted code; use standard pull_request. Do not expose secrets to test environments unnecessarily. |
</threat_model>

<verification>
Check `.github/workflows/ci.yml` for validity.
</verification>

<success_criteria>
GitHub Actions configuration is created, fully specifying the matrix and testing tools.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-quality-gates/03-02-SUMMARY.md`
</output>
