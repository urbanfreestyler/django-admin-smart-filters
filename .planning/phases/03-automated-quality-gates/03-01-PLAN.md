---
phase: "03-automated-quality-gates"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["pyproject.toml", "tests/conftest.py"]
autonomous: true
requirements: ["REL-03"]
must_haves:
  truths:
    - "Test suite completes without errors"
    - "Linter reports zero issues"
    - "Type checker reports zero errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest settings"
      contains: "tool.pytest.ini_options"
  key_links:
    - from: "pyproject.toml"
      to: "tests"
      via: "pytest configuration"
      pattern: "--ds="
---

<objective>
Fix local quality gates so that `pytest`, `ruff`, and `mypy` pass successfully.

Purpose: To ensure the codebase is completely verified and type-safe before cutting a release.
Output: A passing test suite and clean lint/type results.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>task 1: fix pytest configuration and namespace issues</name>
  <files>pyproject.toml</files>
  <action>Fix pytest configuration. Current error is "No module named 'tests.settings'". Update `pyproject.toml`'s `tool.pytest.ini_options.addopts` to point to the correct settings module (likely `test_project.core.settings`). Also fix any broken imports in tests due to the rename to `django_admin_smart_filters`.</action>
  <verify>
    <automated>pytest -v</automated>
  </verify>
  <done>pytest runs successfully without configuration or import errors</done>
</task>

<task type="auto">
  <name>task 2: run and fix ruff linting</name>
  <files>pyproject.toml</files>
  <action>Run `ruff check .` and fix any linting issues. Run `ruff format .` to ensure code is formatted. Modify `pyproject.toml` if any rules need adjusting, though try to fix code first.</action>
  <verify>
    <automated>ruff check .</automated>
  </verify>
  <done>ruff check passes with zero errors</done>
</task>

<task type="auto">
  <name>task 3: run and fix mypy typing</name>
  <files>pyproject.toml</files>
  <action>Run `mypy .` and fix any type-checking errors. The `django-stubs` plugin is configured, so ensure the `django_settings_module` in `pyproject.toml` under `[tool.django-stubs]` matches the fix from task 1.</action>
  <verify>
    <automated>mypy .</automated>
  </verify>
  <done>mypy runs and reports no issues</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local→CI | Local fixes must accurately represent CI environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Test Config | mitigate | Ensure test configuration relies only on standard test packages and is reproducible. |
</threat_model>

<verification>
Run all three tools in succession (`pytest`, `ruff check .`, `mypy .`) and ensure they all pass.
</verification>

<success_criteria>
All code quality tools run successfully on the current codebase without any manual intervention.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-quality-gates/03-01-SUMMARY.md`
</output>
