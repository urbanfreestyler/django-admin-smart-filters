---
phase: 02-build-artifacts-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, requirements-dev.txt]
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "Running the build process produces valid `.tar.gz` (sdist) and `.whl` (wheel) files"
    - "The generated wheel can be installed via `pip` in a clean Python virtual environment"
    - "Importing `django_admin_smart_filters` in the clean environment succeeds without missing dependencies"
  artifacts:
    - path: "dist/"
      provides: "Built distribution artifacts"
  key_links:
    - from: "pyproject.toml"
      to: "django_admin_smart_filters"
      via: "hatchling build config"
      pattern: "django_admin_smart_filters"
---

<objective>
Create and test valid sdist and wheel distributions for the renamed `django_admin_smart_filters` package.

Purpose: Guarantee the package builds correctly and is installable cleanly via pip before pushing to PyPI.
Output: Built distributions in the `dist/` directory and a test verification script/command.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>task 1: Install build tools</name>
  <read_first>requirements-dev.txt</read_first>
  <files>requirements-dev.txt</files>
  <action>Add `build`, `twine`, and `virtualenv` to `requirements-dev.txt` if they are not already present, ensuring we have the tools needed to build the wheel and sdist files.</action>
  <verify>
    <automated>grep -E "build|twine" requirements-dev.txt</automated>
  </verify>
  <acceptance_criteria>requirements-dev.txt contains build and twine</acceptance_criteria>
  <done>Build tools are added to dev requirements</done>
</task>

<task type="auto">
  <name>task 2: Generate distribution artifacts</name>
  <read_first>pyproject.toml</read_first>
  <files>dist</files>
  <action>Run the python build module to create the wheel and sdist files. Use `python -m pip install build` then `python -m build`. Make sure the `dist/` directory contains `django_admin_smart_filters-1.1.0-py3-none-any.whl` and the `tar.gz` file.</action>
  <verify>
    <automated>ls dist/*.whl</automated>
  </verify>
  <acceptance_criteria>dist directory contains .whl and .tar.gz files</acceptance_criteria>
  <done>Artifacts exist in dist/</done>
</task>

<task type="auto">
  <name>task 3: Verify package installability in isolated environment</name>
  <read_first>pyproject.toml</read_first>
  <files>.planning/verify_build.ps1</files>
  <action>Create a test script `.planning/verify_build.ps1` that creates a temporary virtual environment, activates it, pip installs the newly built `.whl` from the `dist/` directory, runs `python -c "import django_admin_smart_filters; print('Success')"` to verify successful import, and then deletes the temporary environment.</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/verify_build.ps1</automated>
  </verify>
  <acceptance_criteria>Script successfully installs the wheel and prints Success.</acceptance_criteria>
  <done>Package is proven to be correctly packaged and cleanly installable.</done>
</task>

</tasks>

<verification>
Check that both sdist and wheel files exist in `dist/`.
Check that the verification script executes successfully without errors.
</verification>

<success_criteria>
- Build process runs without errors.
- Both .tar.gz and .whl are generated.
- The wheel can be installed in a fresh virtualenv.
- `import django_admin_smart_filters` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-artifacts-verification/02-01-SUMMARY.md`
</output>