---
phase: 01-project-rename-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["pyproject.toml", "README.md", "CHANGELOG.md", "LICENSE"]
autonomous: true
requirements: ["REL-01"]
must_haves:
  truths:
    - "Package builds as django_admin_smart_filters"
    - "Metadata shows correct author, version, description, and Python version requirements"
    - "Base documentation (README, CHANGELOG, LICENSE) exists for PyPI rendering"
  artifacts:
    - path: "pyproject.toml"
      provides: "Build configuration and PyPI metadata"
      contains: "django_admin_smart_filters"
    - path: "README.md"
      provides: "Package landing page"
    - path: "CHANGELOG.md"
      provides: "Version history"
    - path: "LICENSE"
      provides: "Open source license"
  key_links:
    - from: "pyproject.toml"
      to: "README.md"
      via: "readme field"
      pattern: "readme = .*"
---

<objective>
Establish the package identity and PyPI-ready metadata for `django_admin_smart_filters`.

Purpose: Ensure the project is installable, has proper metadata (author, version, classifiers) for PyPI, and provides initial landing documentation for users.
Output: A valid `pyproject.toml` with `hatchling` configured, `README.md`, `CHANGELOG.md`, and `LICENSE`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Establish pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>Create pyproject.toml configuring `hatchling` as the build-backend. Define project name "django_admin_smart_filters", version "1.1.0" (per roadmap's v1.1 release readiness), description, authors, MIT license, and Python requirements (>=3.12). Set core dependencies to Django>=5.2 and asgiref>=3.11. Include PyPI classifiers for Django and Python versions. Set up `[tool.hatch.build.targets.wheel]` to include `django_admin_smart_filters`. Add basic configurations for ruff, pytest, and mypy matching the STACK.md versions.</action>
  <verify>
    <automated>python -m site &amp;&gt; /dev/null || echo "Ensure syntax is correct"</automated>
  </verify>
  <done>pyproject.toml accurately reflects the new package name and metadata.</done>
</task>

<task type="auto">
  <name>task 2: Create standard project documentation</name>
  <files>README.md, CHANGELOG.md, LICENSE</files>
  <action>Create a basic README.md with the project name "Django Smart Filters", a brief description, and installation instructions. Create an initial CHANGELOG.md outlining the initial v1.1 release readiness. Create an MIT LICENSE file.</action>
  <verify>
    <automated>pip install -e .</automated>
  </verify>
  <done>Essential files exist for proper PyPI package rendering.</done>
</task>

</tasks>

<verification>
Ensure the project can be installed locally via `pip install -e .` without missing dependencies or syntax errors in `pyproject.toml`.
</verification>

<success_criteria>
- `pyproject.toml` correctly declares the package name as `django_admin_smart_filters`
- Metadata is fully populated for PyPI (author, version, description, classifiers)
- `README.md`, `CHANGELOG.md`, and `LICENSE` exist
- Installation in development mode succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-rename-metadata/01-01-SUMMARY.md`
</output>